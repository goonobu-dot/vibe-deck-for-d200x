{
  "name": "vibe-deck-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local bridge for Vibe Deck D200X agent status",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.8.3"
  }
}
